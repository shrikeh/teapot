language: php

php:
  - 5.3
  - 5.4
  - 5.5

before_install:
  - composer self-update
  - composer validate

install:
  - composer install --prefer-dist

script:
  - vendor/bin/phpcs --standard=PSR2 src -p
