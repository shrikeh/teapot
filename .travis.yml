language: php

php:
  - 5.3
  - 5.4
  - 5.5

matrix:
  allow_failures:
    - php: 5.5
before_script:
  composer install --require-dev
script:
  - phpunit tests/unit/AllTests.php
  - php bin/phpcs --standard=PSR2 --report=summary
