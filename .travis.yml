language: php

php:
  - 5.3
  - 5.4
  - 5.5

script: ant travis

matrix:
  allow_failures:
    - php: 5.5
    - branches:
        except:
          - master
          - stable
          - development
